name = "Elyor"
print(len(name))

some_list = ["some", "name"] # list
print(len(some_list))

# That's polymorphism in action, a single fuction does now
# how to handle different kind of objects as expected!
